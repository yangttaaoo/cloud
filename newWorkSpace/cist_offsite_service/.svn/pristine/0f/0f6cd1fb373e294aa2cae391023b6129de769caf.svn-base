package com.cist.illegalUpload.processor;
import java.util.concurrent.ScheduledThreadPoolExecutor;
import java.util.concurrent.TimeUnit;
import org.springframework.context.ApplicationListener;
import org.springframework.context.event.ContextRefreshedEvent;
import org.springframework.stereotype.Component;

import com.cist.illegalUpload.action.IllegalAutoUploadCallable;
@Component
public class InitConfigProcessor implements ApplicationListener<ContextRefreshedEvent>{
	@Override
	public void onApplicationEvent(ContextRefreshedEvent arg0) {
			/*******违法数据自动上传线程启动******/
//		ScheduledThreadPoolExecutor executor = new ScheduledThreadPoolExecutor(1);
//			executor.scheduleWithFixedDelay(new IllegalAutoUploadCallable(),0, 1000000,TimeUnit.MILLISECONDS);
		}
	}


