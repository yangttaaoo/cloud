package com.cist.alarm.logquery.mapper;

import java.util.HashMap;
import java.util.List;

import com.cist.alarm.logquery.model.Alarm_log;

public interface LogMapper {
	
   List<Alarm_log> selectData(HashMap<String, Object> map);
   
   Integer deleteAll(String[] pk);
   
   Integer deleteOne(Integer pk);
}
