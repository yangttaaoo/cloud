<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cist.controlCarAudit.mapper.ControlCarAuditMapper" >
	<update id="AuditControlCar" parameterType="java.util.HashMap">
	update  JCBK_CONTROL_VEHICLE set 
	shr=#{shr},bkzt=#{bkzt},shsj=SYSDATE
	<if test="bkzt==0">
	,shbtgyy=#{shbtgyy,jdbcType=VARCHAR}
	</if>
	where pk=#{pk}
	</update>
</mapper>